const jsonUrl='https://cyk.moe/qinput/versions.json';async function LoadVersions(){try{const response=await fetch(jsonUrl);const data=await response.json();const container=document.getElementById('versions');const htmlContent=data.versions.map(version=>{const filesHtml=version.files.map(file=>{return`<a href="${file.file}">${file.name}</a>`}).join('\n                ');return`<div class="version"><h2>${version.title}</h2><div class="note left_border">${version.note}</div>${filesHtml}</div>`}).join('');container.innerHTML=htmlContent}catch(error){console.error('获取或解析 JSON 失败:',error)}}